import styled from 'styled-components';

export const CabinetBase = styled.div`
  position: relative;
  display: block;
  border: 2px solid red;
  height: 317px;
  .viz-wrap--logo {
    max-width: 175px;
  }
`;
